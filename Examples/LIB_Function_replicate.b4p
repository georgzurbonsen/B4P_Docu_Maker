#!/usr/local/bin/b4p
               echo( replicate( Café, 10 ) );
               echo( replicate characters( Café, 10 ) );
               echo( replicate ( { Café, Caffè }, 5 ) );       // Vectorization
