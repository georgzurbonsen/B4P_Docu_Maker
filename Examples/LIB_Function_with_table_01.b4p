#!/usr/local/bin/b4p
  table load( cities, "Cities.csv" );

  for (a[]=1, a[]<=4, a[]++)
      with table( cities, 2, a[]  )
      {
          echo("Header  : ", [.,0], new line, "Contents: ", [.], new line );
      }
