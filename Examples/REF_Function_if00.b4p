  for (a[] = 0, a[] <= 6, a[]++)
  {
      if     ( even(a[])) echo(a[], " is even" );
      unless ( a[] < 5  ) echo(a[], " is not smaller than 5" );
      else:  echo (a[], " is smaller than 5");
  }
