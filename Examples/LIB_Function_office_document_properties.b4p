#!/usr/local/bin/b4p
  office document properties( "Football Membership List.xlsx", property names[], property values[] );

  for all parameters( property names[], pn[], index[] )
  {
      echo( str( pn[], "16#", align right), "  :  ", property values[]{index[]} );
  }
