#!/usr/local/bin/b4p
  ex[] = "sqrt( x[]*x[] + y[]*y[] )";
  x[] = 3;
  y[] = 4;

  r[] = expression( sqrt( x[]*x[] + y[]*y[] ) );
  echo("Output r[] = ", r[] );

  r[] = expression( : ex[] );
  echo("Output r[] = ", r[] );

  // Note the required colon.  Otherwise ex[] as variable is returned.
