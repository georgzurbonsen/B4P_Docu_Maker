#!/usr/local/bin/b4p
  v[] = "b[]";

  assign(  c[]  , 456 );
  assign( :v[]  , 123 );
  assign( :"d[]", 789 );
  // Note the required colon.  Otherwise v[] is the variable to assign to.

  echo("Variable b[] = ", b[] );
  echo("Variable c[] = ", c[] );
  echo("Variable d[] = ", d[] );
