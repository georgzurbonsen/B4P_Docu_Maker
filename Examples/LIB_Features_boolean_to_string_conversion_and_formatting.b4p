#!/usr/local/bin/b4p
      for all parameters( { en_us, de_de, fr_fr }, locale[] )
      {
          print( str(true, "Tt Yy # ", locale[]),"   " );
          echo( str(false, "TT y # ", locale[]) );
      }
