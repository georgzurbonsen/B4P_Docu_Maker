#!/usr/local/bin/b4p
               text[] = compose( Hello, new line, Everyone, new line, "!!!" );
           echo(text[], new line );
               echo( "Width  = ",  width(text[]), new line, "Length = ", length(text[]) );

