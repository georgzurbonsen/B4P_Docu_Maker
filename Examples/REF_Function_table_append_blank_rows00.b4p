
  table initialize( animals,
      { { Animal, size }, { ant, tiny }, { mouse, small } } );

  table append blank rows( animals, 2 );

  table append( animals,
      { { rat, not that small }, { horse, big } } );

  table list ( animals );
