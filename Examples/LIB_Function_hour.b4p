#!/usr/local/bin/b4p
               a[] = "14. July 2020, 15:30:40";
               echo( hour( a[] ), ", ", minute( a[] ), ", ", second( a[] ) );
               echo( hour( { a[], "12:00" } ) ); // Vectorization: 2 values
