#!/usr/local/bin/b4p
    do
    {
        echo("Enter a character between 'a' and 'e'. 'x' = exit");
        ch[] = getch ignore case( abcdex );
        echo("Character entered: ", ch[] );
    } while (ch[] <> X);
    echo("Done.");
