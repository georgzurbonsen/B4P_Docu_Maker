#!/usr/local/bin/b4p
               echo( pow( 7, 2 ) );
               echo( pow( 3, 3 ) );
               echo( pow( 81, 0.5 ) );
               echo( pow( 27, 1/3 ) );
               echo( pow( 2, {2,3} ) ); // Vectorized
               echo( pow( {2,3}, 2 ) ); // Vectorized
               echo( pow( {2,3}, {3,4} ) ); // Vectorized both parameters

