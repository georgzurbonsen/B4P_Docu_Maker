#!/usr/local/bin/b4p
               a[] = { 1, 1.5, 2, 3, 4. -1, -1.5, -2 };
               for all( a[], p[] )
               {
                       print(p[],": is ");
                       if (even (p[])) print ("even, ");
                       if (odd  (p[])) print ("odd, ");
                       if (whole(p[])) print ("whole, ");
                       echo;
               }
               echo("Vectorization example: ", whole(a[]) );
