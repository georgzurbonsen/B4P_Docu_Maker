#!/usr/local/bin/b4p
       a[] = { a,b,c, {d,e,f}, g };                      // Assign a set to variable a[]
       a[]{1} = B;
       a[]{-1} = {G,H,I};
       echo( "Modified value in a[] = ", a[] );
