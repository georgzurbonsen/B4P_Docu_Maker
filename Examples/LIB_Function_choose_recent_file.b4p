#!/usr/local/bin/b4p
       a[] = choose recent file( "", "", "*table*.b4p", first, 1 ); // No user interaction
       echo("First file in alphabetic order containing 'table' is ", a[] );

       a[] = choose recent file( "", "", "*table*.b4p", last, 1 ); // No user interactino
       echo("First file in alphabetic order containing 'table' is ", a[] );

       a[] = choose recent file( "Please choose one of the newest files: ", "", "*.b4p", newest, 5 );

       echo("Chosen file is: ", a[] );
