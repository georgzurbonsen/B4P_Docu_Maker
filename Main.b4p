

	table initialize ( table, {{ Last Name, First Name}, 120:{ Mack, Nick }} );

	table save excel file( table, Table, Test.xlsx );

	table save excel file( table, Table, Test.xlsx );

	table save excel file( table, Table, Test.xlsx );

	table save excel file( table, Table, Test.xlsx );

	table save excel file( table, Table, Test.xlsx );

	table save excel file( table, Table, Test.xlsx );
