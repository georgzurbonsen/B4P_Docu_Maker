#!/usr/local/bin/b4p

table load excel file	( t, "Examples\Cities.xlsx");

[t:20,0] = Lone Header;

table style auto width ( t, min, 2 );
table style rows( t, 3, table, text color, red );


table save( t, Test.xls, EXCEL );

